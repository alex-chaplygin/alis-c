CFLAGS=-I. -I../

all: /tmp/test_sound /tmp/test_mouse

/tmp/test_sound: test_sound.c ../sound.c
	gcc $(CFLAGS) -o $@ $^

/tmp/test_mouse: test_mouse.c ../mouse.c
	gcc $^ -o $@ $(CFLAGS) -I/usr/include/SDL2 -lSDL2
